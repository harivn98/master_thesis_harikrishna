services:
  frontend:
    image: sam3/frontend
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sam3_workflow
    shm_size: 16g
    ipc: host
    environment:
      - HF_HOME=/root/.cache/huggingface
      - HUGGINGFACE_HUB_CACHE=/root/.cache/huggingface
      - TRANSFORMERS_CACHE=/root/.cache/huggingface
      - PYTHONPATH=/workspace/sam3
      - NVIDIA_VISIBLE_DEVICES=all
    volumes:
      - ./:/workspace/sam3
    working_dir: /workspace/sam3
    command: ["/bin/bash"]
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    stdin_open: true
    tty: true
